#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
// #define x 'x'
int main()
{

//t1
//  int i,j,m,n;
//  i=8;
//  j=10;
//  m=++i;
//  n=j++;
//  printf("%d,%d,%d,%d\n",i,j,m,n);
//  //t2
//  int a,b,c,d;
//  float c2=(float) c;
//  a=100;
//  b=30;
//  d=a%b;
//  c2=(float)a/b;
//  printf("a=%d,b=%d,c=%f,d=%d\n",a,b,c2,d);
//  return 0;
    //  t2
// float height=1.72;
// int weight=70;
// float BMI=weight/(height*height);
// printf("%.2f",BMI);
// return 0;
        //t3
// float number;  
// printf("输入一个带一位或两位小数的正实数。");  
// scanf("%f", &number);  
// printf("%d",(int)(number+0.5));
    //t4
    //  int a, b;
	//  scanf("%d %d",&a,&b);
	//  printf("a=%d,b=%d\n",a,b);
    // float x=3.5,y=6.2,z=6.8;
    //  x=y;
    //  y=z;
    //  z=x;
    //  printf("x=%f,y=%f,z=%f\n",x,y,z);
    // return 0;
    // printf(“a=\”%d\”,b=\”%d\”\n”,a,b);
    //要实现输入1234，返回结果为a="12",b="34"就必须要规定输入整数的位数://  scanf("%2d%2d",&a,&b);，这样机器才能区分输入的整数是赋给谁的，否则就会1234 然后一直回车，除非输入第二个数
    //  int a, b;
	//  scanf("%2d%2d",&a,&b);
    // scanf("%d%*c%d",&a,&b);
    // printf("a=\"%d\",b=\"%d\"",a,b);
    // 当限定用户输入数据以逗号为分隔符，即输入数据格式为12,34  [回车]时，应修改程序中的哪条语句？怎样修改？ 
    // scanf(“%d,%d”,&a,&b);这时候就不需要2d了，因为机器能通过，区分两个整数分别是赋给谁的
    // double c;
    // scanf("%lf",&c);
    // printf("%f",c);
    
}




